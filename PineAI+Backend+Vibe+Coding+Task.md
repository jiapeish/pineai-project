# PineAI Backend Vibe Coding Task

基于任意你熟悉的后端框架和Golang，实现一个**多模型托管与推理服务平台**，支持动态模型注册与更新，并对接第三方 LLM 实现流式响应。

## 🧩 任务描述

你需要实现一个后端服务，支持以下功能：

### 🧱 核心要求（必须完成）：

### 1. **模型注册与管理**

- 提供模型注册接口：支持注册 `model_name` + `version` + `backend_type`（可选标记是否为模拟模型或真实对接 OpenAI/Ollama 等）
- 支持查看当前模型列表与各版本状态（是否可用、是否在使用中、是否被删除）
- 模型一旦注册成功，可以立即用于推理调用

### 2. **流式推理 API**

- 提供接口：`POST /infer`，输入包含：

```
{
  "model": "chat-bot",
  "version": "v1",
  "input": "Tell me a joke"
}

```

- 后端以**流式方式**（任意协议，根据题目要求进行技术选型判断，在代码中以comments注目设计意图和选型逻辑）返回模型输出内容，模拟 LLM 分词输出；
- 模型推理逻辑可以为：
    - 调用 OpenAI 等第三方接口，或
    - 使用 `time.sleep` 模拟词语生成过程（例如每 200ms 生成一个 token）

### 3. **支持热更新模型**

- 实现 `PUT /models/{name}/version/{v}` 接口支持模型版本热更新；
- 模型更新后，应对**新请求生效**，同时不影响**正在进行推理的旧连接**；
- 系统需要合理隔离“加载中 / 使用中 / 可替换”等状态，避免热更新中断现有请求。

## 🔧 技术约束

- 数据可内存维护（无需持久化），需使用线程安全结构；
- 服务需支持并发多个模型实例、多个请求；
- 每次推理调用都需指定模型名与版本；
- 所有输出需通过 **流式接口返回，模拟 LLM 推理过程**。

## ✅ 验收标准

| **模块** | **要求概述** |
| --- | --- |
| 模型注册/更新能力 | 支持动态注册、查看、更新，不影响 stream 请求 |
| 推理接口 | 支持流式返回 |
| 并发支持 | 并发多个请求、多个模型间不冲突 |
| 热更新稳定性 | 更新期间已有连接不报错、不被终止 |
| API 规范 | 请求体、返回格式清晰，REST 语义合理 |

## 🌟 额外加分项（实现任意一项均为加分）

1. **容量管理**：每个模型最多允许 N 个并发请求（可配置），超出返回错误或排队策略；
2. **多版本分流策略**：同一模型的多个版本可设定流量权重，支持按 hash / 权重路由；
3. **异常处理机制**：支持错误模拟（如超时、无响应），输出错误码 / trace_id / 日志；
4. **Prometheus metrics**：暴露 `/metrics` 接口，输出每个模型 QPS、平均响应延迟、失败率；
5. **简易管理面板**（可用终端 UI 或网页）：展示模型、版本、状态、当前活跃连接数。

## 🎯 optional 进阶挑战题（选做）

以下题目非强制，可选其一：

### 🧠 A. 多后端模型动态切换（如 OpenAI + Ollama + Qwen）

实现一套注册时即可指定 `backend_type` 的机制，并在 `/infer` 时自动选择对应后端驱动进行推理，支持：

- `mock`：使用模拟输出
- `openai`：调用 OpenAI API
- `ollama`：调用本地推理服务
- `qwen`：调用通义千问本地模型

**需确保：**

- 每个后端抽象统一接口；
- 多个模型间切换不影响流控/调度。

### 🧠 B. 模型热重启与资源回收机制

支持：

- 模型长时间未调用自动卸载（释放资源）；
- 若再次调用则触发“延迟加载”机制；
- 状态变更过程中，系统仍保持服务不中断。

### 🧠 C. 模型版本灰度发布

实现以下策略：

- 默认使用 v1；
- 设置 v2 为 `shadow` 模式：用户仍收到 v1 响应，但服务后台也执行 v2 推理，并记录差异日志；
- 便于后续模型质量评估（例如 token 长度差异 / 响应时间差异 / log 内容对比等）。

---

## 📦 交付要求

- 提交一个可运行的项目文件夹（支持本地启动）；
- 提供：
    - [README.md](http://readme.md/)（说明如何注册模型、调用推理、热更新等）
    - `curl` 调用示例或 Postman collection；
    - 实现代码（推荐模块分层清晰，如 handler/router/model_registry/streamer）

## 🕐 时间说明与提交规范

- 初次提交：限时2小时，无论进度如何，都需要提交一份当前仓库内容
- 核心功能提交：限时4 小时内完成主要功能提交
- **进阶项项提交：可使用额外时间，最晚于收到题目当晚12点前提交**

核心功能提交时请附带以下内容：

- 代码仓库链接 或 zip 包
- 本地运行说明（依赖、启动方式）
- 使用的语言与框架说明
- 如有 mock 第三方 LLM，可注明逻辑

## 📝 Self Report（请务必填写）

最终提交版本中，请在README.md中填写以下内容

```
## Self Report

- 总耗时：______ 小时
- 实际做题时间段：____ ~ ____
- 完成情况：
  - [x] 模型注册 / 更新 / 查看
  - [x] 流式推理接口
  - [x] 热更新不影响已有连接
  - [ ] 多版本分流
  - [ ] Prometheus metrics
  - [ ] 灰度发布
- 备注说明：
  - （如对模型状态隔离的实现方式说明 / 关键设计决策 / 哪块逻辑写得不满意 / 下一步想优化的点）

```